{
  "name": "create_function_app",
  "description": "Create a function app",
  "strict": false,
  "parameters": {
    "type": "object",
    "properties": {
      "resource_group_name": {
        "type": "string",
        "description": "The name of the resource group."
      },
      "storage_account_name": {
        "type": "string",
        "description": "The name of the storage account."
      },
      "function_app_name": {
        "type": "string",
        "description": "The name of the function app."
      },
      "location": {
        "type": "string",
        "description": "The region where the function app is deployed."
      },
      "hosting_plan": {
        "type": "string",
        "description": "The name of the hosting plan.",
        "enum": ["Consumption", "Basic", "Standard", "Premium", "ElasticPremium"]
      },
      "runtime_stack": {
        "type": "string",
        "description": "The runtime stack of the function app.",
        "enum": ["dotnet", "node", "java", "powershell", "python", "custom"]
      },
      "runtime_version": {
        "type": "string",
        "description": "The version of the runtime stack."
      }
    },
    "required": [
      "resource_group_name",
      "storage_account_name",
      "function_app_name",
      "location"
    ],
    "additionalProperties": false
  }
}