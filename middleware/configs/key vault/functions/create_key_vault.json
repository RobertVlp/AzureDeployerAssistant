{
  "name": "create_key_vault",
  "description": "Create a key vault.",
  "strict": false,
  "parameters": {
    "type": "object",
    "properties": {
      "resource_group_name": {
        "type": "string",
        "description": "The name of the resource group where the key vault will be created."
      },
      "key_vault_name": {
        "type": "string",
        "description": "The name of the key vault to create."
      },
      "location": {
        "type": "string",
        "description": "The Azure region where the key vault will be deployed (e.g., 'eastus', 'westeurope')."
      },
      "tenant_id": {
        "type": "string",
        "description": "The ID of the Azure Active Directory tenant associated with the key vault."
      }
    },
    "required": [
      "resource_group_name",
      "key_vault_name",
      "location"
    ],
    "additionalProperties": false
  }
}