{
  "name": "get_virtual_network_info",
  "description": "Provide information about a virtual network.",
  "strict": true,
  "parameters": {
    "type": "object",
    "properties": {
      "resource_group_name": {
        "type": "string",
        "description": "The name of the resource group."
      },
      "virtual_network_name": {
        "type": "string",
        "description": "The name of the virtual network."
      }
    },
    "required": [
      "resource_group_name",
      "virtual_network_name"
    ],
    "additionalProperties": false
  }
}