{
  "name": "create_storage_account",
  "description": "Create a storage account.",
  "strict": true,
  "parameters": {
    "type": "object",
    "properties": {
      "resource_group_name": {
        "type": "string",
        "description": "The name of the resource group."
      },
      "storage_account_name": {
        "type": "string",
        "description": "The name of the storage account."
      },
      "location": {
        "type": "string",
        "description": "The region where the storage account is deployed."
      }
    },
    "required": [
      "resource_group_name",
      "storage_account_name",
      "location"
    ],
    "additionalProperties": false
  }
}